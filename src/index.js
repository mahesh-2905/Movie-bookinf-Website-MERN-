import React from 'react';
import ReactDOM from 'react-dom';
import Router from './Components/Routings/routing';

ReactDOM.render(
  <React.StrictMode>
    <Router/>
  </React.StrictMode>,
  document.getElementById('root')
);

